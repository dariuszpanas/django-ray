# Deny by default. The application image needs only these reviewed inputs.
**
!Dockerfile
!pyproject.toml
!uv.lock
!README.md
!docker-entrypoint.sh
!src/
!src/**
!testproject/
!testproject/**

# Dockerfile-specific policies replace the root .dockerignore. Keep local
# environment and database files excluded after broad directory negations.
**/.env
**/.env.*
**/*.sqlite3
