# Django-Ray ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: django-ray-config
  namespace: django-ray
data:
  DJANGO_SETTINGS_MODULE: "testproject.settings"
  # Exercise fail-closed Django configuration checks in the render reference.
  # This mode does not certify the sample manifests as a production topology.
  DJANGO_DEPLOYMENT_MODE: "production"
  DJANGO_DEBUG: "False"
  DJANGO_ALLOWED_HOSTS: "django-ray.example.com"
  # Ray configuration
  RAY_ADDRESS: "ray://ray-head-svc:10001"
  DJANGO_RAY_WORKING_DIR_URI: "file:///runtime-env/django-ray-source.zip"
  DJANGO_RAY_RECOVERY_WORKING_DIR: "/runtime-env/django-ray-recovery.zip"
  DJANGO_RAY_INPUT_STORAGE_BACKEND: "filesystem"
  DJANGO_RAY_INPUT_STORAGE_FILESYSTEM_PATH: "/payload-storage/inputs"
  RAY_DASHBOARD_HOST: "0.0.0.0"
  RAY_DASHBOARD_URL: "http://localhost:30265"
  RAY_STATE_API_ADDRESS: "http://ray-head-svc:8265"
  # Bundled database configuration for evaluation and local validation only.
  DATABASE_ENGINE: "django.db.backends.postgresql"
  DATABASE_NAME: "django_ray"
  DATABASE_HOST: "postgres-svc"
  DATABASE_PORT: "5432"

