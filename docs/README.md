# Documentation source

The canonical published entry point is [index.md](index.md). This short file exists
only to orient people browsing the `docs/` directory on GitHub and is intentionally
omitted from the site navigation.

Keep public navigation and guide summaries in `index.md` and `zensical.toml` instead of
maintaining a second copy here.
