# Deny by default. The legacy Ray image needs only these reviewed inputs.
**
!Dockerfile.ray
!pyproject.toml
!uv.lock
!README.md
!src/
!src/**
!testproject/
!testproject/**

# Dockerfile-specific policies replace the root .dockerignore. Keep local
# environment and database files excluded after broad directory negations.
**/.env
**/.env.*
**/*.sqlite3
