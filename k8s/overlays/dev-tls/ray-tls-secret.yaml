# Ray TLS Certificates Secret for development
# Generated using scripts/generate-ray-tls-certs.sh
#
# DO NOT commit real certificates to version control!
# Create the live Secret from ignored certificate files during deployment:
#
#   kubectl create secret generic ray-tls-certs \
#     --namespace=django-ray \
#     --from-file=ca.crt=./certs/ray-tls/ca.crt \
#     --from-file=tls.crt=./certs/ray-tls/tls.crt \
#     --from-file=tls.key=./certs/ray-tls/tls.key
#
# This reference is not a Kustomize resource. Never write private keys here.

